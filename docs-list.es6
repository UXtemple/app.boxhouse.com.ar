import createListOf from './create-list-of';

const { actionTypes, actions, reducer: baseReducer } = createListOf('DOCS_LIST');

function reducer(state, {type, value}) {
  case boxActionTypes.

}

export { actionTypes, actions, reducer };
